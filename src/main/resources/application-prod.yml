spring:
  datasource:
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    hikari:
      # Supabase 무료 티어 연결 수 제한 대응
      maximum-pool-size: 5
      minimum-idle: 1
      connection-timeout: 10000
  jpa:
    show-sql: false
